- change around how texturing in the shader works
	- THANK YOU tiawl for showing me their code that will let one sample a texture
	- but because I started working on texturing i found some bugs and some issues with my current texturing setup
	- I have to change so that on the cpu i do not set the transparent flag based on materials color (because that doesnt take into account the texture), instead add it as an option to materials some sort of toggle.
	- the default for this toggle will be opaque (so the flag will be set as 0) but then the dev can set it to transparent to enable alpha testing
	- I am thinking because if a dev enables alpha blending on something that is actually opaque, then it should still not spawn another ray? or do i just spawn another ray and everything gets messed up? lol idk yet but it feels like if alpha testing is enabled then i still check to see the texture and material's alpha value before spawning a new ray?
	- is it possible that just naively spawning a ray wont mess with the output color but will just cause a lot of extra performance (which is why its opt in and default opaque)