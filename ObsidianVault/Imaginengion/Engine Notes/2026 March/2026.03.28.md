- need to ensure that when creating scenes and entities that if they have a uuid that it gets properly added to Serializer's UUID map
	- currently it is still on the old system where the SceneManager held the UUIDs
- change all the calls to GetUUID to use the new function attached to Serializer instead
---
- Change Serializer
	- change so that serializer has a like "CurrentDeserializeContext" which is a ResolveContext = struct { requester: Requester, comp_ptr: \*anyopaque}
		- requester is an entity + uuid + set loc variable
		- comp_ptr is a pointer to the component we are currently parsing
	- note: that now instead of adding a blank component during the jsonParse we can AddComponent a blank component before hand, set it to be part of the context (comp_ptr) and then after parsing assign it to the data inside the pointer to the added component.
		- the reason we need to do the assignment still instead of just directly assigning fields in jsonParse is because not all components have a jsonParse and for those we NEED to do an assignment so we still need to keep that
		- This means that actually we will entity.AddComponent(.{}) AND we will need to create a blank component inside of jsonParse both will be needed which makes parsing the jsonParse ones a tiny bit slower but it is what it is i suppose. Also this is only for deserializing text, for binary in shipped games it will not need this
	- This can probably scale to multithreading in the future as well..? but its complicated lmao 
	- Added new hashmaps one for each world, and then added a comptime variable to know which world hashmap to modify.
	- This fits the pattern of the rest of the engine when having a general system do work with specific worlds. 
---
- So changing the pattern of serializer did not end up working because it was kind of impossible to tell which kind of world we were dealing with so I had to move UUID stuff into SceneManager and then update creation/deletion/deserilization of entites and scenes to use SceneManager instead. 
- With the other changes i made earlier to deserialization (like having a deserlized context) + the fact that entity/scenes have a reference to the scene manager themselves it actually ends up working out well anyway
---
- changed callback_list in EventManager to use std.DoublyLinkedList which makes everyhting much cleaner but I was just thinking about actually how it might be useless because the top most callback that is provided can trickle down the event to the sub structs/functions anyway so actually EventManager may only need to take in 1 single callback. 
	- The reason why it supports multiple callbacks is because when cleaning up at the end of frame, the SceneManager wants to be notified of ECS events, and of course the ECS its self wants to know too. But if i simply only use SceneManager callback (instead of scene manager and ecs) then I can still call the ECS Event function from the SceneManager since the SceneManager owns it anyway. 
	- This is actually the point of this kind of event architecture but idk i just felt like it needed to support multiple callbacks when the top level one can do the trickling down its self anyway.
	- The reason why I wanted to support multiple callback functions is because I was thinking of the ECS as if it were standalone and it would definately want to have its own internal event callback but what if the user of the ECS wants to provide their own function to callback to? then both can happen. This way the ECS behavior is consistant and isnt reliant on the user of the ECS knowing that like if they want to set their own callback they also have to handle calling into the ECS as well after handling it themselves, rather the ECS always takes care of its self