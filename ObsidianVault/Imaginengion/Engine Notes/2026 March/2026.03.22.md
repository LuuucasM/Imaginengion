- change to a freelist system for my ECS and then i can remove the "removeIDS list"
	- making my own SparseSet implementation
		- Things I need to finish:
		- fix that when removing an entity, the id's generation is increased in the DenseToSparse array
		- add a function to get the next free entity id
- add generations to ECS to safely reuse IDs
- I Created my own sparse set because I wanted to integrate generations directly into the sparse set. I also added a free list functionality which is inspired by EnTT. It is also nice to be able to control the exact internals. I think I also left room for extendibility in the future.
- 
---
- change how serializing and deserializing work
	- Now on deserialize if there is an entity_id saved then create a resolve ID object and add it to an array
	- at the end of the full deserialization go through each possible resolve and try to resolve UUIDs to world IDs
		- first for each resolve we have to check if the requesting entity id is still valid. if its not then remove from the array (probably do a swap with the last one and dont increment i)
		- if we find it then good just resolve
		- if we cant find it its ok then just go to the next one. since we check if the requesting entity is valid first, its ok to just leave it around because if the requesting entity goes invalid then we will remove it anyway (thanks to generations)