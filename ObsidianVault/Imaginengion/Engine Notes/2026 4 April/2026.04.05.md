- started changing the UI a whole bunch
	- the reason is since I am sort of consolidating player, scene, entity, gamemode (new entity type) so I am also trying to have their behavior the same rather than different systems
- because of this i got rid of the individual scene panel and instead now I have ECSDisplay which is just a general purpose panel for display all the objects in an ECS. 
	- the one bad thing about this system right now is that entities are usually associated with a specific scene in normal engine hierarchies. This means you can easily open a scene hierarchy and see the entity/entity hierarchy as well
	- since everything is kind of just separate right now it is impossible to tell which scene an entity belongs to
	- I want to change it so that scenes get different colored borders and entities can then also get the same colored border?
- Finish adding run settings drag drop target
- configure ECSDisplay again so that each Trait has its own HandleDragDropSource function