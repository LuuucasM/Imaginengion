- separate lens component into view point component and render target components
	- this is makes it cleaner so that you can have like the position and rotation and what not of a camera from the actual frame buffer since sometimes you want there to be a "camera" but you dont need to be holding gpu resources so its just separating the gpu resouces from the camera
	- then also adding render target component to hold those gpu resources and you can add them where you need