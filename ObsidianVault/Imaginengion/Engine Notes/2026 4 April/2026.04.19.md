- change asset manager to allow assets that are not from file but are generated from a descriptor
	- This change will let me use Texture2D with frame buffers
	- with texture2d in frame buffers it lets me much easier display frame buffers to the screen
		- note i am going to be displaying frame buffers directly on the screen based on the imgui panel dimentions
		- this way I can avoid having to write a new pipeline just for some UI when it will get changed later down the road anyway
		- right now i am basically changing the code to be able what i want to do later but much earlier than expect which is why things are so broken right now
	- since frame buffers can hold texture2d directly now, that means quads can reference textures that the frame buffer directly draws to
	- i am modifying the asset manager to help me with this because if i tried to use just raw pointers or something then making sure im accessing valid pointers would be a nightmare where as the asset system was literally built for ensuring reference stays valid for as long as it needs to
- OR THIS IS WHAT I THOUGHT
- see the problem i was trying to fix is that quad needs a stable reference to the texture to use in rendering, and the texture also needs to register its self to the bindless system. 
- so originally I was thinking I could have frame buffers have texture2d's but then i was like if I want stable references I should just go full asset system. 
- BUT I WAS WRONG
- Because frame buffers exist in a RenderTargetComponent, and RenderTargetcomponents are attached to (either entities or players) actually the entity (and its component) is the stable reference that i needed and is way cleaner because I dont have to juggle this weird psudo asset in the asset manager and things stay stable and healthy
- I will keep the code if written for the asset manager for now as computed assets are actually a real thing afaik but this was not actually the true usecase for them.
---
- gosh i found out that actually you CAN just pass an \*SDL_GPUTexture as the texture id so i dont need any of what i developed today technically but actually the things I have half implemented today can be used later its just I actually dont need it now