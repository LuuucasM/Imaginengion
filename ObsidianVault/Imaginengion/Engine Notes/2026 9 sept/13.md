**AUDIO-1 — Double load/clamp/store per mixed sample in the mixing loop**

|            |                                                                                                             |
| ---------- | ----------------------------------------------------------------------------------------------------------- |
| Location   | `src/Imaginengion/AudioManager/AudioManager.zig:75-81`                                                      |
| Frequency  | Per sample, per playing audio entity, every `OnUpdate`                                                      |
| Impact     | Doubles memory traffic on the mix buffer in the innermost audio loop                                        |
| Confidence | High                                                                                                        |
| Fix        | `mixed_buffer[i] = std.math.clamp(mixed_buffer[i] + source, -1.0, 1.0);` instead of separate add-then-clamp |
| Verify     | Profile mixing loop with several concurrent sources; expect fewer loads/stores                              |
|            |                                                                                                             |
|            |                                                                                                             |

**AUDIO-2 — Per-sample function-call indirection for trivial scalar ops**

|            |                                                                                             |
| ---------- | ------------------------------------------------------------------------------------------- |
| Location   | `src/Imaginengion/AudioManager/AudioManager.zig:76-80, 86-92`                               |
| Frequency  | Per sample, per playing audio entity, every `OnUpdate`                                      |
| Impact     | Real per-sample call overhead in Debug/ReleaseSafe; likely inlined in ReleaseFast           |
| Confidence | Low-Medium                                                                                  |
| Fix        | Mark `SourceVolume`/`ClampMix` `inline fn`, or fold into the loop body (pairs with AUDIO-1) |
| Verify     | Inspect generated IR/assembly in target build mode      

**ECS-2 — Group-query skip-field mask recomputed at runtime despite being fully compile-time-determined**

|            |                                                                                                                                                    |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Location   | `src/Imaginengion/ECS/ECSManager.zig:123-136`, mask logic in `ComponentManager.zig:163-193`                                                        |
| Frequency  | Once per query per frame                                                                                                                           |
| Impact     | Rebuilds a `StaticSkipField` union/intersect/difference from scratch every call for a value that's identical every time given the comptime `query` |
| Confidence | High                   


**ECSMA-3 — Redundant `Timestamp.now()` call inside pending-delete loop**

| | |
|---|---|
| Location | `src/Imaginengion/ECSManagers/AManager.zig:302-315` |
| Frequency | Per frame, once per entry in `mPendingDelete` |
| Impact | Extra clock read per entry instead of once per frame |
| Confidence | High |
| Fix | Hoist `Timestamp.now()` above the `while` loop and reuse for all entries |
| Verify | Trivial — inspect call count before/after |

**ECSOB-5 — `IsActive` always performs a manager lookup even when the ID is already known invalid**

|            |                                                                                                                   |
| ---------- | ----------------------------------------------------------------------------------------------------------------- |
| Location   | `src/Imaginengion/ECSObjects/ECSObject.zig:228-246` (`Core.IsActive`)                                             |
| Frequency  | Very high — gates most component operations (`GetComponent`, `AddComponent`, etc.)                                |
| Impact     | An avoidable manager-side lookup runs unconditionally even for an already-invalid ID                              |
| Confidence | Medium                                                                                                            |
| Fix        | `if (!self.IsIDValid()) return false;` before calling into the manager, instead of computing both unconditionally |
| Verify     | Static — control flow no longer calls the manager for invalid IDs                                                 |

- change ECS Objects, ECS Managers, and ECS Components to unify them and use comptime generation when possible
	- this follows in line with the formalization that was established as part of the UI rework
	- I think by making these changes I am sort of "choosing" how my engine will be aka the heavy use of ECS for different game features. 
	- This is me just further committing to the goal of the engine where one of the pillars is ECS and the other is Events
	- I am not actually changing any behavior but just again unifying everything to use comptime generation for shared parts (which there is a lot of since they all share the same generic ECS). This will result in less code to debug and more consistent rules across the different game objects.
- Finish changing ECSObjects to use "Core" for shared functinality
- ensure ECSObjects have their unique functions preserved
- change all ECSManagers to use ECSObjects. 
	- I am not sure if I should make a generic ECSMangaer as well.
	- It would be basically the same thing as ECSObjects in that certain things would be shared between managers but there might be object specific implementation details and member functions
- Ensure that asset manager retains the same ability that the current asset manager has
- Modify various event callback functions in the engine to instead of returning bool it now returns an "EventResult" enum
	- instead of just putting true or false and having to remember, it is easier to reason what is happening with enum names.
	- In case I need to add more types of results later I can easily as well